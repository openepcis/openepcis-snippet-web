{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "prod": "nuxt start",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "rm -rf  .nuxt .output node_modules package-lock.json .data dist .idea .vscode"
  },
  "devDependencies": {
    "nuxt": "^4.0.3",
    "@nuxt/ui": "^3.3.0"
  },
  "dependencies": {
    "@heroicons/vue": "^2.2.0",
    "@headlessui/vue": "^1.7.23",
    "@vueuse/core": "^11.3.0"
  }
}
